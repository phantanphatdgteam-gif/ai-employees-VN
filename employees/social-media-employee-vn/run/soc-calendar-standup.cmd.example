:: «SOC_ROOT»\run\soc-calendar-standup.cmd
:: Copy this file to soc-calendar-standup.cmd (drop the .example), replace «SOC_ROOT» with your real folder, and check the
:: path to claude.exe: this is where the native installer puts it (where.exe claude shows yours).
:: Never point it at the Desktop app's bundled copy; Task Scheduler cannot see that path.
:: Section 10 of CAPABILITIES.md: a scheduled run must never wait on a prompt. If this routine stalls
:: or is refused a tool under acceptEdits, change it to bypassPermissions for this one file, knowing
:: what that grants. The guardrails live in the contract and the routine, not in this flag.
@echo off
cd /d "«SOC_ROOT»"
"%USERPROFILE%\.local\bin\claude.exe" -p "Read «SOC_ROOT»/routines/soc-calendar-standup/SKILL.md and follow it." --permission-mode acceptEdits --output-format json < nul > "«SOC_ROOT»\run\soc-calendar-standup.last.json"
if errorlevel 1 node "«SOC_ROOT»\scripts\runlog.mjs" --failed-run soc-calendar-standup --exit-code %errorlevel%
